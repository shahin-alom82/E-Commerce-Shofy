"use client"
import CartProducts from "@/components/cart/CartProducts";
const ProductsPage = () => {
      return (
            <div>
                  <CartProducts />
            </div>

      );
};

export default ProductsPage;